language: bash
deploy:
  -
    provider: script
    script: ./deploy $DEPLOYKEY_DEV
    on:
      repo: SvenDub/RankingGame
      branch: develop
  -
    provider: script
    script: ./deploy $DEPLOYKEY
    on:
      repo: SvenDub/RankingGame
      branch: master